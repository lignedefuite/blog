---
title: Tags
---

Blowfish 支持基于 Hugo 的所有分类方法。同时，当前的标签预览页也支持展示自定义内容。

在这里可以为每个分类添加额外的描述信息。查看下面的[高级标签]({{< ref "advanced" >}})页面，了解更多。

注意，这个页面的原语言是en，只是为了调试的方便，被修改为了中文内容。

因为目前只有将语言设置为en时，能够正常看到tag。

或许将默认语言设置为zh-cn时，tags将只会在zh-cn的页面中显示。

我想这是tags本身的内容也需要设置多个语言方案的缘故。

---
